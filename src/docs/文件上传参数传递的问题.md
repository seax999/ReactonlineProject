##### 文件上传文件流参数传递的问题

> 描述：

> 文件上传获取的是 `FileItem`，文件流传递到后端的参数需要是 `binary` 类型。如过需要额外参数，则需要通过 `formDate.append` 添加参数。
> 且 `FormData` 类型的参数在 `post` 请求中不能 `JSONStringify` 序列化

> 代码

```js
const formData = new FormData();
formData.append("file", file);
formData.append("workId", "123");
```
